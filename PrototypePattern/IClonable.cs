namespace PrototypePattern
{

    public interface IClonable
    {
        /* Summary:
            Creates a new object that's a copy of the current instance.
         */
        object Clone();
    }
}